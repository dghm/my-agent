Square icon-only button for toolbars, close affordances, and nav chrome; always pass `label`.

```jsx
<IconButton label="關閉" variant="ghost"><XIcon /></IconButton>
```

Variants: `ghost` (default), `outline`, `solid`. Sizes: sm 28 / md 36 / lg 44px — lg meets touch-target minimums.
