Radio option — parent owns group state.

```jsx
<Radio label="Air freight" checked={mode === "air"} onChange={() => setMode("air")} />
<Radio label="Sea freight" checked={mode === "sea"} onChange={() => setMode("sea")} />
```
